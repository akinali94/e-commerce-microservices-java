# Redis specific configuration
spring.profiles=redis

# Detailed Redis logging if needed
logging.level.org.springframework.data.redis=INFO
logging.level.com.example.cartservice.repository.RedisCartRepository=DEBUG