# Server configuration
server.port=9562

# Service integration
services.frontend.url=${FRONTEND_URL:http://localhost:3000}
services.productcatalog.url=${PRODUCT_CATALOG_SERVICE_ADDR:http://localhost:9561}

# Recommendation configuration
recommendations.max=5

# Logging configuration
logging.level.root=INFO
logging.level.com.example.recommendationservice=INFO

# Monitoring
management.endpoints.web.exposure.include=health,info,metrics